[pytest]
python_files = *.py
markers =
log_format = %(asctime)s %(levelname)s %(message)s
log_date_format = %Y-%m-%d %H:%M:%S
# if you need to collect logs during the test, change this to true
log_cli = True
log_level = INFO
